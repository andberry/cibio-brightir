{% extends "base/base.twig" %}
{% from "components/hero.twig" import hero %}

{% block extrahead %}
<script src='https://api.mapbox.com/mapbox-gl-js/v2.9.1/mapbox-gl.js'></script>
<link href='https://api.mapbox.com/mapbox-gl-js/v2.9.1/mapbox-gl.css' rel='stylesheet' />
{% endblock %}

{% block main %}
  {{hero(hero.title, hero.subtitle, hero.image)}}
  <div class="c-container--small">
    <div class="my-16 xl:my-32 prose prose-lg max-w-none">
      {{ content }}
    </div>

    {#
    <div class="my-24">
      <div class="max-w-screen-md">
      <iframe data-tally-src="https://tally.so/embed/3NoMVj?alignLeft=1&hideTitle=1&transparentBackground=1&dynamicHeight=1" loading="lazy" width="100%" height="1" frameborder="0" marginheight="0" marginwidth="0" title="Contact form"></iframe>
      <script>var d=document,w="https://tally.so/widgets/embed.js",v=function(){"undefined"!=typeof Tally?Tally.loadEmbeds():d.querySelectorAll("iframe[data-tally-src]:not([src])").forEach((function(e){e.src=e.dataset.tallySrc}))};if("undefined"!=typeof Tally)v();else if(d.querySelector('script[src="'+w+'"]')==null){var s=d.createElement("script");s.src=w,s.onload=v,s.onerror=v,d.body.appendChild(s);}</script>
    </div>
    #}
  </div>
{% endblock %}



{#
<div class="aspect-[2/1] my-16">
    <div id='map' style='width: 100%; height: 100%;'></div>
  </div>
</div>

<script>
  const mapPins = {{map.pins|json_encode}};
  mapboxgl.accessToken = 'pk.eyJ1IjoiYW5kYmVycnkiLCJhIjoiY2xyZjhocHU5MDJrZDJxb2J6eTRpMDRkeCJ9.iKAviDzwPmSjHizlEzlhcg';
  const map = new mapboxgl.Map({
    container: 'map',
    style: 'mapbox://styles/mapbox/light-v11',
    projection: 'mercator',
    minZoom: 1
  });
  map.addControl(new mapboxgl.NavigationControl(), 'top-left');
  map.addControl(new mapboxgl.ScaleControl());

  // add markers
  const mapBounds = new mapboxgl.LngLatBounds();
  for (const pin of mapPins) {
    new mapboxgl.Marker({color: pin.color})
      .setLngLat([pin.lng, pin.lat])
      //.setPopup(new mapboxgl.Popup().setHTML("<h1>Hello World!</h1>")) // add popup
      .setPopup(new mapboxgl.Popup().setHTML(`<h3 class="font-bold text-xl pr-12">${pin.title}</h3><p>${pin.content}</p>`))
      .addTo(map);
    mapBounds.extend([pin.lng, pin.lat]);
  }
  map.fitBounds(mapBounds, {padding: 100, animate: false, maxZoom: 18});
  
</script>
#}